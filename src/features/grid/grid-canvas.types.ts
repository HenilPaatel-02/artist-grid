import { GridLine } from "./grid.types";

export type CanvasSize = {
  width: number;
  height: number;
};

export type GridCanvasLayout = {
  width: number;
  height: number;
};

export type GridCanvasStyle = {
  color: string;
  strokeWidth: number;
};

export type ScaledGridLine = GridLine;
